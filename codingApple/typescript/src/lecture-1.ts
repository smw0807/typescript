type members = string | number | boolean;
let members: members[] = ['song', 'kim', 'park', 10, false];

let memberObj: { member1: string; member2: string } = { member1: 'a', member2: 'b' };

let myName: string = 'Song Min Woo';
let myAge: number = 31;
let myPlace: string = 'Hwaseong';

const like: { song: string; singer: string } = {
  song: 'crazy',
  singer: 'Aerosmith',
};

const project: { member: string[]; days: number; started: boolean } = {
  member: ['kim', 'park'],
  days: 30,
  started: true,
};
